@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


<div>
    <div>
        <ng-content></ng-content>
    </div>
    <div *ngIf="pagescount>1">
        <button (click)="Back()"  class="btn btn-default">Назад</button>
        <button class="btn btn-default" *ngFor="let n of buttons let i=index" (click)="ChangePage(n)" >{{n}}</button>
        <button (click)="Next()"  class="btn btn-default" >Вперед</button>
    </div>
</div>